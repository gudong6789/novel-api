<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AdminApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminManager"
        android:usesCleartextTraffic="true">

        <!-- 主界面 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 授权码管理界面 -->
        <activity
            android:name=".ui.AuthCodeManageActivity"
            android:exported="false" />

        <!-- 设备管理界面 -->
        <activity
            android:name=".ui.DeviceManageActivity"
            android:exported="false" />

        <!-- 全局配置界面 -->
        <activity
            android:name=".ui.GlobalConfigActivity"
            android:exported="false" />

        <!-- 数据统计界面 -->
        <activity
            android:name=".ui.StatisticsActivity"
            android:exported="false" />

    </application>

</manifest>
